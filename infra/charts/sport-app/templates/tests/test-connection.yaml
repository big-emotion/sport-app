apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sport-app.fullname" . }}-test-connection"
  labels:
    {{- include "sport-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sport-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
