server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/sport_db
    username: sport_user
    password: sport_pass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
